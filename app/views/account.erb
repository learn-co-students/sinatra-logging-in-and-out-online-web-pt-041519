<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">  
  </head>
  <body id="page-top">
    <% if Helpers.is_logged_in?(session) %>
        <h1><%= "Welcome #{@current_user.username}" %></h1></br></br>
        <h3><%= "Your Balance: #{@current_user.balance}" %></h3></br></br>
        <h2><a href="/logout">Log Out</a></h2>
    <% else %>
        <h2>You Must <a href="/">Log In</a> to View Your Balance</h2>
    <% end %>
    
  </body>
</html>